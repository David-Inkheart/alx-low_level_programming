#include<stdio.h>

/**
 * main - Entry point
 *printout should be -followed by a new line, -all to the standard error.
 * Return: Always 0 (Success)
 */
int main(void)
{
	fprintf(stderr, "and that piece of art is useful\" - Dora Korpar, 2015");
	fprintf(stderr, "-10-19\n");

	return (1);
}
