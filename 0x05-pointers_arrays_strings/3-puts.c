#include "main.h"
#include <stdio.h>

/**
* _puts - writes the string str and a trailing newline to stdout
*
* @str: string to be written out to stdout
*
* Return: Always 0
*
*/
void _puts(char *str)
{
	puts(str);
}
