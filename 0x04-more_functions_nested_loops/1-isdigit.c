#include "main.h"
/**
 * _isdigit - checks for lowercase character c
 * @c: The character to print
 * int c - a random number from '0' to '9''
 * Return: 1 if character is lowercase, 0 if not.
 */
int _isdigit(int c)
{

	if (c >= '0' && c <= '9')
	return (1);
	return (0);
}
