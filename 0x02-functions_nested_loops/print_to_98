#include <stdio.h>

/**
 * main - entry point
 *
 *
 *Return: int
 */
int main(void)
{
	if (n >= 98)
	{
	while (n > 98)
	printf("%d, ", n--);
	printf("%d\n", n);
	}

	else
	{
	while (n < 98)
	printf("%d, ", n++);
	printf("%d\n", n);
	}
}
