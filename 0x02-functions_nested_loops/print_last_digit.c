#include "main.h"
/**
 * main - Entry point
 *
 * Description: 'prints the last digit of a number'
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	l;

	if (n < 0)
	{
	l = -1 * (n % 10);
	_putchar(l + '0')
	return (l);
	}
	else
	{
	l = n % 10;
	_putchar(l + '0');
	return (l);
	}
}
